apiVersion: v2
name: sms-checker
description: Minimal chart for model-service and app
type: application
version: 1.0.0
appVersion: "1.0.0"

dependencies:
  - name: kube-prometheus-stack
    version: ">=55.0.0 <56.0.0"
    repository: "https://prometheus-community.github.io/helm-charts"
    alias: monitoring